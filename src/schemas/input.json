{
  "$schema": "http://json-schema.org/draft-07/schema",
  "id": "http://chapivia.online/schemas/input.json",
  "title": "Input",
  "description": "User input",
  "type": "object",
  "properties": {
    "email": { 
      "type": "string",
      "pattern": "^$|^[a-zA-Z0-9@*#!$.]+$"
    },
    "name": {
      "type": "string",
      "pattern": "d",
      "maxLength": 256
    },
    "password": { 
      "type": "string", 
      "pattern": "^$|^[a-zA-Z0-9@*#!$.]+$"
    },
    "confirm password": { 
      "type": "string", 
      "pattern": "^$|^[a-zA-Z0-9@*#!$.]+$"
    },
    "guess": {
      "type": "string",
      "pattern": "^$|^[a-dA-D]+$",
      "maxLength": 1
    }
  },
  "errorMessage": {
    "properties": {
      "name": "Name should only conatin letters and numbers",
      "email": "Please enter address in the correct format example@example.com",
      "password": "Password should only conatin letters, numbers, symbols: ! @ # $ %",
      "confirm password": "Confirm password should only conatin letters, numbers, symbols: ! @ # $ %",
      "guess": "Guess should only be one character: A B C D"
    }
  }
}
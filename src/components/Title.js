import React from 'react';
import { FlexItem, Text } from './elements';
import { css } from 'styled-components';

export default (props) => {
  return (
    <FlexItem>
      <Text.p fontSize="3rem">
        Chapivia
      </Text.p>
    </FlexItem>
  )
}